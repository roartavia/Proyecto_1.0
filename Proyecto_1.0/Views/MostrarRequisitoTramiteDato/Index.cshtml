@model Proyecto_1._0.Models.TipoTramite
@using System.Data

@{
ViewBag.Title = "Index";
}

<h2>Mostrar Requisito Tramite Dato</h2>


@using (Html.BeginForm())
{
    <div class="form-group">
        @Html.LabelFor(model => model.id_tramite, "id_tramite", htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.DropDownList("id_tramite", null, htmlAttributes: new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.id_tramite, "", new { @class = "text-danger" })
        </div>
    </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Solicitar" class="btn btn-default" />
            </div>
        </div>
}
