@model IEnumerable<Proyecto_1._0.Models.TipoServicio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Nombre Institución")
        </th>
        <th>
            @Html.DisplayName("Nombre Servicio")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instituciones.nombre_institucion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_servicio)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_servicio }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_servicio }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.id_servicio })
        </td>
    </tr>
}

</table>
